pipeline {
	agent any
	tools {
       	 maven 'mvn-3.5.0'
    	 }
	stages {
		stage ('build') {

           	steps {
                	echo '****************STARTING BUILD*******************'
               		sh 'mvn package'
        	    }
     		   
			}
		}
	}
